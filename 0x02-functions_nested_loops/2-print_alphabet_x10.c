#include "main.h"

/**
 * print_alphabet_x10 - prints 10 times the alphabet
 *
 * return: 0 always
*/

void print_alphabet_x10(void)
{
	int line;
	int ch;

	for (line = 1; line <= 1; line++)
	{
		for (ch = 97; ch <= 122; ch++)
		{
			_putchar(ch);
		}
		_putchar('\n');
	}
}
